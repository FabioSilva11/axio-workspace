<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingTop="8dp"
    android:paddingEnd="24dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/pix_qr_code"
        android:layout_width="220dp"
        android:layout_height="220dp"
        android:layout_gravity="center_horizontal"
        android:adjustViewBounds="true"
        android:contentDescription="@string/plans_pix_qr_description"
        android:scaleType="fitCenter" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/plans_pix_instructions"
        android:textColor="@color/chat_text_secondary"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/pix_copy_code"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:background="@color/chat_surface_soft"
        android:ellipsize="middle"
        android:maxLines="3"
        android:padding="12dp"
        android:textColor="@color/chat_text_primary"
        android:textIsSelectable="true"
        android:textSize="12sp" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/pix_open_link"
        style="@style/Widget.Material3.Button.OutlinedButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="@string/plans_pix_open_link" />

    <TextView
        android:id="@+id/pix_status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/plans_payment_pending"
        android:textColor="@color/chat_text_secondary"
        android:textSize="13sp" />

</LinearLayout>
